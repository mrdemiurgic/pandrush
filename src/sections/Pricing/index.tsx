import React from 'react';

import { Container, InnerContainer, Banner } from './styles';

const Pricing = () => {
  return (
    <Container id="pricing">
      <InnerContainer>
        <Banner>Pricing</Banner>
      </InnerContainer>
    </Container>
  );
};

export default Pricing;
