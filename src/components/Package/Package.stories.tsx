import React from 'react';

import VideoPackage from './index';

export default {
  title: 'Components/VideoPackage',
  component: VideoPackage,
};

const Template = (args) => <VideoPackage {...args} />;

export const Default = Template.bind({});
